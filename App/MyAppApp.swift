import MyAppUI
import SwiftUI

/// Application entry point — wiring only. All real code lives in Packages/MyAppKit.
@main
struct MyAppApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
